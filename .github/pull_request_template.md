## What changed and why

<!-- Describe the change and the problem it solves. Link related issues if any. -->

## Checklist

- [ ] No personal data was added (no real name, email, phone, address, or application folder —
      see `CONTRIBUTING.md`).
- [ ] CI checks pass (structure, gitleaks, PII scan, Python compile/validation).
- [ ] Docs updated if this changes observable behaviour (`README.md`, `automation/README.md`,
      `CONTRIBUTING.md`, or the relevant agent/rule file).
- [ ] Verification commands were actually run — paste them and their output below.

## Verification

<!--
Run whichever apply to your change and paste real output, e.g.:

python scripts/check_latex_toolchain.py
python scripts/latex_healthcheck.py <folder>
python -m watcher.poll --dry-run
python scripts/init_workspace.py --verify
python automation/hooks/guard.py --self-test
-->
