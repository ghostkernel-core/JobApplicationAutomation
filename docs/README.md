# Documentation

Start here: **[Getting Started](./getting-started.md)** — clone, run `init_workspace.py`, run the
`12-workspace-init` agent, finish the manual steps, and produce your first application.

- [Getting Started](./getting-started.md) — prerequisites, setup checklist, first end-to-end run.
- [The Pipeline](./the-pipeline.md) — the 11 pipeline steps, the agent roster, model routing,
  the payload/prose split, the rules system, honesty design, and the deliverable folder layout.
- [The Watcher](./watcher.md) — configuring `sources.toml` and `config.toml`, running the
  watcher, replying on Telegram, and the digest/heartbeat/learning loop.
- [Customising](./customising.md) — editing the LaTeX templates, the `identity.toml` placeholder
  mechanism, fonts, the `rules/` files, and the tracker scripts.
- [Privacy and Security](./privacy-and-security.md) — the `.gitignore` whitelist model, the
  headless-build sandbox, secret handling, and the CI PII scan.
- [Troubleshooting](./troubleshooting.md) — symptom → cause → fix for the problems you are most
  likely to hit.
