# Slice: Final Verifier

Run after: application `.tex`/`.pdf` pairs (2 EN-only, 4 with German) + Interview Prep
`.tex`/`.pdf` + 1 posting `.html` exist AND `scripts/latex_healthcheck.py` passes.

## Check list

1. **Deliverables**: application `.tex`+`.pdf` pairs (2 or 4) + Interview Prep `.tex`+`.pdf` + posting `.html`.
2. **Folder cleanliness**: remove or request removal of all intermediate payloads, API captures, raw text extracts, logs, temporary JSON, and build artifacts from the deliverable folder before PASS. The final folder must not contain `application_payload.json`, `cv_payload_*.json`, `letter_payload_*.json`, `interview_prep_payload_en.json`, `posting.txt`, `detail.json`, `list.json`, `.aux`, `.log`, `.out`, or compiler working directories.
3. **Page limits**: CV/Lebenslauf ≤2 pages; letters ≤1 page each. Interview Prep has no strict page cap (reference material, not judged on length).
4. **No defects**: no placeholders (`{{...}}`, `TODO`, `TBD`, `??`), stale dates, mixed company names, old role text, empty bullets — applies to all documents including Interview Prep.
5. **Integrity (CV/letter only)**: no unsupported claims/tools, invented metrics, visa/permit/sponsorship/relocation language, doctorate unless required. All facts trace to `rules/00-canonical-profile.md`. **Exempt: Interview Prep** — it is private prep, not an application document, and is expected to name unclaimed tools, gaps, and visa/logistics topics deliberately as do-not-claim guidance. Do not flag that content.
5b. **No AI fingerprints (CV/Lebenslauf/letter/Anschreiben — rule 07 section F, hard fail)**: grep extracted PDF text case-insensitively for authorship signatures and tool credits ("generated with", "generated by", "AI-generated", "AI-assisted", "written by AI", "drafted with"), assistant voice ("As an AI", "Certainly!", "I hope this helps"), meta-commentary ("This letter highlights…", "Tailored for:", "Note:", "Draft v2"), bracketed placeholders, and model/AI-vendor names: Claude, GPT/ChatGPT, Sonnet, Opus, Haiku, Anthropic, OpenAI, Codex, Gemini, Bard, Gemma, DeepMind, Llama, Mistral, Mixtral, Cohere, Grok, xAI, Perplexity, Copilot, Cursor, Ollama, LM Studio, LangChain, MCP, or any other assistant/foundation-model brand. Generic terms (LLM, NLP, computer vision, PyTorch) pass. A banned word passes only inside the employer's role title, company name, or product name. Also confirm no such string sits in PDF metadata or a filename. **Exempt: Interview Prep** — it may name models and vendors freely; authorship signatures still not allowed there.
6. **German quality**: correct umlauts (ä ö ü ß), no ASCII transliterations, `Sie` form, `Mit freundlichen Grüßen`.
7. **Cross-document consistency**: same facts in EN/DE, same name/address/phone/email, same date on all docs.
8. **PDF quality**: text extractable, consistent across EN/DE documents. Visually inspect all PDFs for left alignment/ragged-right body text, natural word spacing, uniform wrapped-line gaps, and no centered-looking or over-justified paragraphs. Cover letters may keep the sender-address block right aligned; all other letter text must be left aligned.
9. **Letter spacing**: English and German cover letters must have visible paragraph separation/double-line style gaps between body paragraphs, not dense single-block text.

## PASS / FIXED / REJECTED

Return verdict with concise findings. If LaTeX/PDF defects survive healthcheck, tell orchestrator to fix locally — do not debug LaTeX structure here.

See `rules/05-integrity-no-fabrication.md`, `rules/06-folder-and-naming.md`, `rules/07-humanlike-anti-ai.md`, `rules/04-german-market-rules.md`.
